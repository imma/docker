#!/usr/bin/env bash

function _docker_profile {
  local shome="${_docker_home:="$(cd -P -- "$(dirname -- "$BASH_SOURCE")/.." && pwd -P)"}"
  PATH="$shome/bin:$PATH"

  DOCKER_VERSION='17.06.0-ce'
  DOCKER_UBUNTU_VERSION='17.06.0~ce-0~ubuntu'
}

_docker_profile
